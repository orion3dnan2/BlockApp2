You are a senior full-stack engineer. The previous agent only created documentation files instead of applying real changes. Now, you MUST modify the actual project files directly. Do NOT generate guide files, do NOT add Markdown notes. Only update the real source code.

Your mission now is:

────────────────────────────
  1) SWITCH PROJECT TO MYSQL/WAMPSERVER COMPLETELY 
────────────────────────────
The project MUST run on MySQL (MariaDB) inside WampServer on Windows. 
You must update REAL FILES:

- drizzle.config.ts
- server/db.ts (or database file)
- shared/schema.ts
- server/index.ts
- package.json
- client config

Apply MySQL conversion INSIDE the code, NOT in documentation files.

Required REAL actions:
- Remove all PostgreSQL imports.
- Add mysql2 + drizzle-orm/mysql2.
- Update drizzle connection to MySQL.
- Convert ALL schema fields to MySQL-compatible types:
    serial → int autoIncrement
    uuid → varchar(36)
    timestamp/timestamptz → datetime
    numeric → decimal
    boolean → tinyint(1)
- Ensure migrations and db:push work.

────────────────────────────
  2) PREPARE COMPLETE PRODUCTION BUILD
────────────────────────────
You must make the app run in production mode on Windows using Wamp as reverse proxy.

Required modifications:
- Express MUST serve the built client from /dist.
- Add proper static build handling.
- Add fallback route for SPA.
- Update package.json scripts to:

    "dev": run dev mode
    "build": build client + server
    "start": run production backend serving built frontend

- Ensure no dev-only code is left in production.

────────────────────────────
  3) LOCAL DOMAIN INSTEAD OF LOCALHOST
────────────────────────────
You MUST implement real changes:

- Add APP_URL to .env usage.
- Configure backend CORS to accept APP_URL.
- Configure frontend base URL to APP_URL.
- Remove any hard-coded localhost or IP.

Example APP_URL:
    http://blocksystem.local

────────────────────────────
  4) FINAL DELIVERABLE
────────────────────────────
You MUST output ONLY:

1. All modified code files with real implemented changes.
2. No markdown guides.
3. No explanations.
4. Only the final modified source code for each file.

If any file requires creation (like db.ts), create it.

DO NOT WRITE DOCUMENTATION.
DO NOT SUMMARIZE.

APPLY REAL CHANGES TO THE PROJECT NOW.
