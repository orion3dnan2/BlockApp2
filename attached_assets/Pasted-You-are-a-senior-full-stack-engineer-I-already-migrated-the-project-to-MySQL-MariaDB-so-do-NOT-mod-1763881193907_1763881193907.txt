You are a senior full-stack engineer. I already migrated the project to MySQL/MariaDB, so do NOT modify the database layer again. Now complete the remaining setup to make the system fully production-ready on a Windows PC using WampServer64 and accessible through a local domain name.

Required tasks:

1) Local domain instead of localhost/IP:
   - Add APP_URL in .env (example: http://blocksystem.local)
   - Configure client to use APP_URL for all API requests.
   - Configure server to:
        • trust APP_URL
        • set correct CORS origin = APP_URL
        • respond to requests from the local domain.
   - Ensure frontend routing works when opened from the domain name.

2) Wamp VirtualHost configuration:
   - Generate the correct VirtualHost configuration for Apache:
        <VirtualHost *:80>
            ServerName blocksystem.local
            ProxyPass / http://127.0.0.1:5000/
            ProxyPassReverse / http://127.0.0.1:5000/
        </VirtualHost>
   - Make sure the system does NOT require PHP (pure Node.js reverse proxy).
   - Update documentation for Windows hosts file:
        127.0.0.1 blocksystem.local

3) Production build finalization:
   - Modify package.json scripts:
        "dev": run dev server
        "build": builds backend + client
        "start": runs production server and serves built client
   - Ensure Express serves the Vite client build from /dist.
   - Remove any Vite dev-only configs.
   - Fix any TypeScript build errors.

4) Environment & configuration cleanup:
   - Update .env.example with:
        APP_URL="http://blocksystem.local"
        DATABASE_URL="mysql://root:@localhost:3306/blocksystem"
        SESSION_SECRET="your-secret"
        PORT=5000
   - Ensure both frontend and backend read APP_URL correctly.

5) Output:
   - Show me all modified files.
   - Show the final package.json scripts.
   - Show server code used to serve static production build.
   - Show frontend config pointing to APP_URL.
   - Provide full Windows + Wamp instructions:
        • Edit hosts file
        • Add VirtualHost
        • Restart Apache
        • Run npm install
        • Run npm run build
        • Run npm start
        • Open http://blocksystem.local

Important:
- Do NOT modify anything related to database migration again.
- Do NOT remove or change the system logic.
- Only finalize production setup, local domain, CORS, routing, and build.
