# 📘 دليل تشغيل المشروع على سيرفر محلي مع WampServer

## 📋 المتطلبات الأساسية

### 1. WampServer64
- **التحميل:** https://www.wampserver.com/en/
- **الإصدار الموصى به:** أحدث إصدار (64-bit)
- **يتضمن:** Apache + MySQL (MariaDB) + PHP

### 2. Node.js
- **التحميل:** https://nodejs.org/
- **الإصدار الموصى به:** v20.x LTS
- **ملاحظة:** اختر النسخة LTS (Long Term Support)

### 3. Git (اختياري)
- **التحميل:** https://git-scm.com/
- **الاستخدام:** لتحميل المشروع من GitHub

---

## 🚀 الخطوة 1: تثبيت WampServer

### 1.1 تحميل وتثبيت
```
1. اذهب إلى: https://www.wampserver.com/en/
2. اضغط على "DOWNLOAD"
3. اختر "Wampserver 64 bits"
4. قم بتشغيل الملف المحمل
5. اتبع خطوات التثبيت (Next → Next → Install)
```

### 1.2 التحقق من التثبيت
```
1. ابحث عن أيقونة WampServer في شريط المهام
2. اضغط عليها بالزر الأيسر
3. إذا كانت خضراء = كل شيء يعمل ✅
4. إذا كانت صفراء/برتقالية = بعض الخدمات متوقفة ⚠️
5. إذا كانت حمراء = خطأ في الخدمات ❌
```

### 1.3 تشغيل الخدمات
```
اضغط بالزر الأيمن على أيقونة WampServer
→ Start All Services
→ انتظر حتى تصبح الأيقونة خضراء
```

### 1.4 اختبار MySQL
```
1. افتح المتصفح
2. اذهب إلى: http://localhost/phpmyadmin
3. إذا فتحت صفحة phpMyAdmin = MySQL يعمل ✅
```

---

## 🚀 الخطوة 2: تثبيت Node.js

### 2.1 تحميل وتثبيت
```
1. اذهب إلى: https://nodejs.org/
2. حمّل النسخة LTS (مثلاً: 20.x.x LTS)
3. قم بتشغيل الملف المحمل
4. اتبع خطوات التثبيت (Next → Next → Install)
5. أعد تشغيل الكمبيوتر بعد التثبيت
```

### 2.2 التحقق من التثبيت
```powershell
# افتح PowerShell أو Command Prompt
# تحقق من نسخة Node.js
node --version
# يجب أن يظهر: v20.x.x

# تحقق من نسخة npm
npm --version
# يجب أن يظهر: 10.x.x
```

---

## 🚀 الخطوة 3: تحميل المشروع

### 3.1 إنشاء مجلد المشروع
```powershell
# افتح PowerShell
# اذهب إلى المكان المناسب (مثلاً: المستندات)
cd C:\Users\YourUsername\Documents

# أنشئ مجلد للمشاريع
mkdir projects
cd projects
```

### 3.2 نسخ ملفات المشروع
```
طريقة 1: باستخدام Git
─────────────────────
git clone [رابط المشروع]
cd blocksystem

طريقة 2: التحميل اليدوي
───────────────────────
1. حمّل المشروع كملف ZIP من Replit
2. فك الضغط في مجلد projects
3. أعد تسمية المجلد إلى blocksystem
```

### 3.3 فتح المشروع
```powershell
cd C:\Users\YourUsername\Documents\projects\blocksystem

# أو استخدم مستكشف الملفات:
# انتقل للمجلد → اضغط Shift + زر الماوس الأيمن
# → "Open PowerShell window here"
```

---

## 🚀 الخطوة 4: إنشاء قاعدة البيانات

### 4.1 فتح phpMyAdmin
```
1. افتح المتصفح
2. اذهب إلى: http://localhost/phpmyadmin
3. ستظهر لوحة التحكم
```

### 4.2 إنشاء قاعدة البيانات
```sql
1. من القائمة اليسرى، اضغط على "New" (جديد)
2. في خانة "Database name" اكتب: blocksystem
3. في "Collation" اختر: utf8mb4_unicode_ci
4. اضغط "Create" (إنشاء)

✅ يجب أن ترى قاعدة البيانات "blocksystem" في القائمة اليسرى
```

### 4.3 التحقق
```sql
-- في تبويب SQL، نفذ هذا الأمر:
SHOW DATABASES LIKE 'blocksystem';

-- يجب أن تظهر نتيجة:
-- blocksystem
```

---

## 🚀 الخطوة 5: إعداد المشروع

### 5.1 إنشاء ملف .env
```powershell
# في مجلد المشروع، أنشئ ملف .env
# يمكنك استخدام Notepad أو أي محرر نصوص

# انسخ المحتوى التالي:
```

```env
# Database Configuration
DATABASE_URL=mysql://root:@localhost:3306/blocksystem

# Application URL
APP_URL=http://localhost:5000

# Server Port
PORT=5000

# Session Secret (غيّره لشيء عشوائي في الإنتاج)
SESSION_SECRET=my-super-secret-key-change-this-in-production

# Node Environment
NODE_ENV=development
```

**ملاحظات مهمة:**
- ✅ `root` = اسم المستخدم الافتراضي في WampServer
- ✅ لا توجد كلمة مرور افتراضياً (لذلك `:@`)
- ✅ `3306` = المنفذ الافتراضي لـ MySQL
- ✅ `blocksystem` = اسم قاعدة البيانات

### 5.2 تثبيت Dependencies
```powershell
# في PowerShell، في مجلد المشروع:
npm install

# انتظر حتى ينتهي التحميل (قد يستغرق 2-5 دقائق)
# يجب أن ترى رسالة:
# "added XXX packages"
```

**إذا ظهرت أخطاء:**
```powershell
# احذف المجلدات القديمة
Remove-Item node_modules -Recurse -Force
Remove-Item package-lock.json -Force

# ثم أعد التثبيت
npm install
```

---

## 🚀 الخطوة 6: إنشاء جداول قاعدة البيانات

### 6.1 تشغيل Drizzle Push
```powershell
npm run db:push
```

**المخرجات المتوقعة:**
```
✓ Pulling schema from database...
✓ Generating migrations...
✓ Applying migrations...
✓ Done!
```

### 6.2 التحقق من الجداول
```
1. افتح phpMyAdmin
2. اختر قاعدة البيانات "blocksystem"
3. يجب أن ترى 4 جداول:
   ✅ users
   ✅ records
   ✅ police_stations
   ✅ ports
```

### 6.3 فحص بنية الجداول
```sql
-- في phpMyAdmin، في تبويب SQL:
DESCRIBE users;
DESCRIBE records;
DESCRIBE police_stations;
DESCRIBE ports;

-- تحقق من أن كل جدول يحتوي على الحقول الصحيحة
```

---

## 🚀 الخطوة 7: تشغيل المشروع (Development)

### 7.1 تشغيل السيرفر
```powershell
npm run dev
```

**المخرجات المتوقعة:**
```
🌱 Starting database seeding...
📍 Inserting police stations...
✅ Inserted 39 police stations
🚪 Inserting ports...
✅ Inserted 11 ports
✅ Database seeding completed!
7:30:00 AM [express] serving on port 5000
```

**✅ إذا شاهدت هذا = النظام يعمل بنجاح!**

### 7.2 فتح التطبيق
```
1. افتح المتصفح
2. اذهب إلى: http://localhost:5000
3. يجب أن ترى صفحة تسجيل الدخول
```

### 7.3 تسجيل الدخول
```
البيانات الافتراضية:
────────────────────
اسم المستخدم: admin
كلمة المرور: Admin@123
```

**بعد تسجيل الدخول:**
- ✅ يجب أن ترى لوحة التحكم
- ✅ يمكنك التنقل بين الصفحات
- ✅ يمكنك إضافة قيود جديدة

---

## 🚀 الخطوة 8: بناء المشروع للإنتاج (Production)

### 8.1 بناء المشروع
```powershell
# أوقف السيرفر أولاً (Ctrl + C)

# قم ببناء المشروع
npm run build
```

**المخرجات المتوقعة:**
```
vite v5.x.x building for production...
✓ 2700 modules transformed.
✓ built in XXs

dist/index.js  XX.Xkb
```

### 8.2 تحديث .env للإنتاج
```env
# غيّر هذا السطر:
NODE_ENV=production
```

### 8.3 تشغيل الإنتاج
```powershell
npm start
```

**المخرجات المتوقعة:**
```
[express] serving on port 5000
```

**افتح:** http://localhost:5000

---

## 🚀 الخطوة 9: (اختياري) إعداد Domain محلي

### 9.1 تعديل ملف hosts
```
1. افتح Notepad كمسؤول (Run as Administrator)
2. اذهب إلى: File → Open
3. انتقل إلى: C:\Windows\System32\drivers\etc\
4. غيّر نوع الملفات إلى "All Files"
5. افتح ملف "hosts"
6. أضف في النهاية:

127.0.0.1 blocksystem.local

7. احفظ الملف (Ctrl + S)
```

### 9.2 تحديث .env
```env
APP_URL=http://blocksystem.local
```

### 9.3 إعداد Apache VirtualHost

**9.3.1 فتح ملف التكوين:**
```
1. اذهب إلى: C:\wamp64\bin\apache\apache2.x.x\conf\extra\
2. افتح ملف: httpd-vhosts.conf
```

**9.3.2 أضف في النهاية:**
```apache
<VirtualHost *:80>
    ServerName blocksystem.local
    
    ProxyPreserveHost On
    ProxyPass / http://localhost:5000/
    ProxyPassReverse / http://localhost:5000/
    
    ErrorLog "logs/blocksystem-error.log"
    CustomLog "logs/blocksystem-access.log" common
</VirtualHost>
```

**9.3.3 تفعيل Proxy Modules:**
```
1. افتح: C:\wamp64\bin\apache\apache2.x.x\conf\httpd.conf
2. ابحث عن هذه الأسطر وتأكد أنها غير معلقة (#):

LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_http_module modules/mod_proxy_http.so

3. احفظ الملف
```

**9.3.4 إعادة تشغيل Apache:**
```
زر الماوس الأيمن على أيقونة WampServer
→ Apache → Service → Restart Service
```

### 9.4 الاختبار
```
افتح المتصفح → http://blocksystem.local
يجب أن ترى التطبيق يعمل!
```

---

## 🔧 حل المشاكل الشائعة

### ❌ المشكلة 1: "Cannot connect to MySQL"
```
الحل:
1. تأكد أن WampServer شغال (أيقونة خضراء)
2. افتح Services (services.msc)
3. ابحث عن "wampmysqld64"
4. تأكد أنه في حالة "Running"
5. إذا لم يكن كذلك، اضغط "Start"
```

### ❌ المشكلة 2: "Port 3306 already in use"
```
الحل 1: إيقاف MySQL الآخر
─────────────────────────
1. افتح Task Manager
2. ابحث عن "mysqld"
3. أوقف العملية

الحل 2: تغيير المنفذ
────────────────────
في .env:
DATABASE_URL=mysql://root:@localhost:3307/blocksystem
```

### ❌ المشكلة 3: "npm: command not found"
```
الحل:
1. أعد تشغيل الكمبيوتر بعد تثبيت Node.js
2. افتح PowerShell جديد
3. حاول مرة أخرى
```

### ❌ المشكلة 4: "Error: connect ECONNREFUSED"
```
الحل:
1. تأكد من DATABASE_URL صحيح في .env
2. تأكد من MySQL شغال
3. اختبر الاتصال:

mysql -u root -p
(اضغط Enter عند سؤال كلمة المرور)

إذا اتصلت بنجاح:
mysql> SHOW DATABASES;
```

### ❌ المشكلة 5: "Table 'blocksystem.users' doesn't exist"
```
الحل:
npm run db:push

إذا استمرت المشكلة:
npm run db:push --force
```

### ❌ المشكلة 6: صفحة بيضاء عند فتح التطبيق
```
الحل:
1. افتح Console في المتصفح (F12)
2. اقرأ الأخطاء
3. عادةً يكون السبب:
   - السيرفر متوقف
   - المنفذ خاطئ
   
4. تأكد من:
   npm run dev شغال
   http://localhost:5000 صحيح
```

---

## 📊 التحقق من نجاح التشغيل

### ✅ Checklist النهائي

- [ ] WampServer شغال (أيقونة خضراء)
- [ ] phpMyAdmin يفتح (http://localhost/phpmyadmin)
- [ ] قاعدة البيانات blocksystem موجودة
- [ ] الجداول الأربعة موجودة (users, records, police_stations, ports)
- [ ] Node.js مثبت (`node --version` يعمل)
- [ ] npm مثبت (`npm --version` يعمل)
- [ ] Dependencies مثبتة (مجلد node_modules موجود)
- [ ] ملف .env موجود وصحيح
- [ ] `npm run dev` يعمل بدون أخطاء
- [ ] التطبيق يفتح على http://localhost:5000
- [ ] يمكن تسجيل الدخول بـ admin/Admin@123
- [ ] يمكن التنقل بين الصفحات
- [ ] يمكن إضافة قيد جديد

**إذا كل هذا صحيح = النظام يعمل بنجاح! 🎉**

---

## 🎯 الخطوات التالية

### 1. إضافة مستخدمين جدد
```
1. سجل دخول كـ admin
2. اذهب إلى الإعدادات
3. تبويب "المستخدمين"
4. اضغط "إضافة مستخدم"
```

### 2. إضافة مخافر ومنافذ جديدة
```
1. سجل دخول كـ admin أو supervisor
2. اذهب إلى الإعدادات
3. تبويب "المخافر" أو "المنافذ"
4. أضف ما تريد
```

### 3. إدخال البيانات
```
1. اذهب إلى "إدخال البيانات"
2. املأ النموذج
3. احفظ
```

### 4. البحث والتقارير
```
1. اذهب إلى "البحث"
2. استخدم الفلاتر
3. شاهد النتائج
```

### 5. استيراد من Excel
```
1. اذهب إلى "استيراد"
2. اختر ملف Excel
3. تأكد من تطابق الأعمدة
4. استورد
```

---

## 📞 الدعم

إذا واجهت أي مشكلة:

1. **تحقق من الـ Logs:**
   ```powershell
   # في PowerShell حيث يعمل npm run dev
   # اقرأ الأخطاء المطبوعة
   ```

2. **تحقق من MySQL Logs:**
   ```
   C:\wamp64\logs\mysql.log
   ```

3. **تحقق من Apache Logs:**
   ```
   C:\wamp64\logs\apache_error.log
   ```

4. **تحقق من Console المتصفح:**
   ```
   اضغط F12 في المتصفح
   → تبويب Console
   → اقرأ الأخطاء
   ```

---

## 🎉 تم بنجاح!

إذا اتبعت كل الخطوات، الآن لديك:
- ✅ نظام إدارة الرقابة والتفتيش يعمل محلياً
- ✅ قاعدة بيانات MySQL متصلة
- ✅ بيانات أولية محملة (مخافر ومنافذ)
- ✅ حساب مدير جاهز
- ✅ جاهز للاستخدام والتطوير

**مبروك! 🎊**
