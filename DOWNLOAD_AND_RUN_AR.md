# 📥 دليل تحميل وتشغيل المشروع على Windows

## 🎯 نظرة عامة

هذا الدليل يشرح كيفية تحميل المشروع من Replit وتشغيله على جهازك مع XAMPP.

---

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

### 1. XAMPP
- التحميل: https://www.apachefriends.org/
- الإصدار المطلوب: أي إصدار يحتوي على MySQL/MariaDB
- **مهم:** يجب أن يكون XAMPP مثبتاً ويعمل

### 2. Node.js
- التحميل: https://nodejs.org/
- الإصدار المطلوب: 20 أو أحدث
- **تحقق من التثبيت:**
  ```bash
  node --version
  npm --version
  ```

### 3. محرر نصوص (اختياري)
- Visual Studio Code: https://code.visualstudio.com/
- أو أي محرر آخر تفضله

---

## 📥 الخطوة 1: تحميل المشروع من Replit

### الطريقة الأولى: تحميل ZIP
1. في Replit، اضغط على زر القائمة (⋮) بجانب اسم المشروع
2. اختر **"Download as zip"**
3. احفظ الملف على جهازك
4. فك ضغط الملف في المكان المطلوب

### الطريقة الثانية: استخدام Git (إذا كان المشروع على GitHub)
```bash
git clone <repository-url>
cd block-system
```

---

## ⚙️ الخطوة 2: إعداد قاعدة البيانات

### 1. تشغيل XAMPP
1. افتح **XAMPP Control Panel**
2. اضغط **Start** بجانب **Apache** (اختياري)
3. اضغط **Start** بجانب **MySQL** (إلزامي)
4. انتظر حتى يتحول اللون إلى أخضر

### 2. إنشاء قاعدة البيانات
1. اضغط **Admin** بجانب MySQL (يفتح phpMyAdmin)
2. في phpMyAdmin:
   - اضغط **New** في القائمة اليسرى
   - اسم قاعدة البيانات: `blocksystem`
   - Collation: `utf8mb4_general_ci`
   - اضغط **Create**

---

## 🔧 الخطوة 3: إعداد المشروع

### 1. فتح Terminal/Command Prompt
1. افتح **Command Prompt** أو **PowerShell**
2. انتقل إلى مجلد المشروع:
   ```bash
   cd C:\path\to\your\project
   ```

### 2. إنشاء ملف البيئة
1. انسخ ملف `.env.example` إلى `.env`:
   ```bash
   copy .env.example .env
   ```

2. افتح `.env` وتأكد من المحتوى:
   ```env
   DATABASE_URL=mysql://root:@localhost:3306/blocksystem
   PORT=5000
   SESSION_SECRET=your-secret-key-here-change-in-production
   NODE_ENV=development
   ```

   **ملاحظات مهمة:**
   - إذا قمت بتعيين كلمة مرور لـ MySQL في XAMPP، غيّر:
     ```
     DATABASE_URL=mysql://root:YOUR_PASSWORD@localhost:3306/blocksystem
     ```
   - لا تترك مسافات قبل أو بعد `=`
   - تأكد من عدم وجود `?sslmode=require` في DATABASE_URL

---

## 📦 الخطوة 4: تثبيت الحزم

في نفس Terminal، نفّذ:

```bash
npm install
```

**المتوقع:**
- سيتم تحميل جميع الحزم المطلوبة
- قد يستغرق 2-5 دقائق حسب سرعة الإنترنت
- إذا ظهرت تحذيرات، يمكن تجاهلها (ما لم تكن errors)

---

## 🗄️ الخطوة 5: إنشاء جداول قاعدة البيانات

نفّذ الأمر:

```bash
npm run db:push
```

**المتوقع:**
```
✓ Done!
```

**هذا الأمر سيقوم بـ:**
- الاتصال بقاعدة البيانات
- إنشاء جميع الجداول المطلوبة:
  - `users` - جدول المستخدمين
  - `police_stations` - مراكز الشرطة
  - `ports` - الموانئ والمنافذ
  - `records` - السجلات

---

## 👥 الخطوة 6: إضافة الحسابات التجريبية

نفّذ الأمر:

```bash
npx tsx server/seed.ts
```

**المتوقع:**
```
🌱 Starting database seeding...
👤 Creating test users...
✅ Admin user created (username: admin, password: admin123)
✅ Supervisor user created (username: supervisor, password: super123)
✅ Regular user created (username: user, password: user123)
...
✨ Database seeding completed successfully!
```

**ملاحظة:** إذا كانت الحسابات موجودة مسبقاً، سيظهر:
```
⚠️  Users already exist. Skipping seed.
```

---

## 🚀 الخطوة 7: تشغيل التطبيق

نفّذ الأمر:

```bash
npm run dev
```

**المتوقع:**
```
> rest-express@1.0.0 dev
> cross-env NODE_ENV=development tsx server/index.ts

[express] serving on port 5000
```

**لا تغلق نافذة Terminal/Command Prompt!**

---

## 🌐 الخطوة 8: فتح التطبيق

1. افتح المتصفح (Chrome, Edge, Firefox)
2. اذهب إلى: **http://localhost:5000**

---

## 🔐 تسجيل الدخول

استخدم أحد الحسابات التجريبية:

### حساب المدير
```
اسم المستخدم: admin
كلمة المرور: admin123
```

### حساب المشرف
```
اسم المستخدم: supervisor
كلمة المرور: super123
```

### حساب الموظف
```
اسم المستخدم: user
كلمة المرور: user123
```

---

## ❗ حل المشاكل الشائعة

### المشكلة 1: `tsx: not found`
**الحل:**
```bash
npm install -g tsx
# أو استخدم
npx tsx server/index.ts
```

### المشكلة 2: `Error: connect ECONNREFUSED 127.0.0.1:3306`
**السبب:** MySQL غير مشغل في XAMPP

**الحل:**
1. افتح XAMPP Control Panel
2. اضغط Start بجانب MySQL
3. انتظر حتى يصبح أخضر
4. أعد تشغيل `npm run dev`

### المشكلة 3: `ER_BAD_DB_ERROR: Unknown database 'blocksystem'`
**السبب:** قاعدة البيانات غير موجودة

**الحل:**
1. افتح phpMyAdmin
2. أنشئ قاعدة بيانات باسم `blocksystem`
3. أعد تشغيل `npm run db:push`

### المشكلة 4: `Port 5000 already in use`
**الحل 1:** غيّر المنفذ في `.env`:
```env
PORT=3000
```

**الحل 2:** أوقف البرنامج المستخدم للمنفذ:
```bash
# ابحث عن البرنامج
netstat -ano | findstr :5000

# أوقف البرنامج (استبدل PID برقم العملية)
taskkill /PID <PID> /F
```

### المشكلة 5: صفحة بيضاء في المتصفح
**الحل:**
1. تحقق من أن التطبيق يعمل في Terminal
2. امسح الـ cache في المتصفح (Ctrl + Shift + R)
3. جرب في نافذة تصفح خاص (Incognito)
4. تحقق من Console في المتصفح (F12)

### المشكلة 6: `Module not found`
**الحل:**
```bash
# احذف node_modules وأعد التثبيت
rmdir /s /q node_modules
del package-lock.json
npm install
```

---

## 🛑 إيقاف التطبيق

لإيقاف التطبيق:
1. اذهب إلى نافذة Terminal/Command Prompt
2. اضغط **Ctrl + C**
3. أكد بـ **Y** إذا سُئلت

---

## 🔄 إعادة التشغيل

لإعادة تشغيل التطبيق بعد الإيقاف:
```bash
npm run dev
```

---

## 📊 التحقق من البيانات

لعرض البيانات في قاعدة البيانات:
1. افتح phpMyAdmin (من XAMPP Control Panel → MySQL → Admin)
2. اختر قاعدة البيانات `blocksystem`
3. اضغط على أي جدول لعرض البيانات

---

## 🔧 الأوامر المفيدة

```bash
# تشغيل التطبيق
npm run dev

# فحص أخطاء TypeScript
npm run check

# تحديث قاعدة البيانات
npm run db:push

# إنشاء migration جديد
npm run db:generate

# إضافة حسابات تجريبية
npx tsx server/seed.ts

# بناء التطبيق للإنتاج
npm run build

# تشغيل نسخة الإنتاج
npm start
```

---

## 📁 هيكل المشروع

```
block-system/
├── client/              # الواجهة الأمامية (React)
│   ├── src/
│   │   ├── components/  # المكونات
│   │   ├── pages/       # الصفحات
│   │   └── contexts/    # Context APIs
├── server/              # الخادم (Express)
│   ├── index.ts         # نقطة البداية
│   ├── routes.ts        # API Routes
│   ├── db.ts            # اتصال قاعدة البيانات
│   ├── auth.ts          # نظام المصادقة
│   ├── storage.ts       # طبقة البيانات
│   └── seed.ts          # البيانات التجريبية
├── shared/              # ملفات مشتركة
│   └── schema.ts        # Schema قاعدة البيانات
├── .env                 # إعدادات البيئة
├── package.json         # حزم npm
└── README_WINDOWS.md    # دليل التشغيل
```

---

## 🎓 خطوات إضافية (اختيارية)

### تغيير المنفذ
1. افتح `.env`
2. غيّر `PORT=5000` إلى أي رقم تريده
3. أعد تشغيل التطبيق

### تغيير اسم قاعدة البيانات
1. أنشئ قاعدة بيانات جديدة في phpMyAdmin
2. غيّر `DATABASE_URL` في `.env`
3. نفّذ `npm run db:push`

### إضافة مستخدمين جدد
يمكنك:
1. استخدام صفحة "إدارة المستخدمين" في التطبيق
2. أو إضافة مباشرة عبر phpMyAdmin

---

## 🆘 الحصول على المساعدة

إذا واجهت مشاكل:

1. **تحقق من السجلات:**
   - راجع رسائل الخطأ في Terminal
   - افتح Console في المتصفح (F12)

2. **راجع الملفات:**
   - `README_WINDOWS.md` - دليل تفصيلي
   - `ACCOUNTS_AR.md` - معلومات الحسابات

3. **تحقق من المتطلبات:**
   - MySQL يعمل في XAMPP
   - Node.js مثبت
   - ملف `.env` موجود وصحيح

---

## ✅ قائمة التحقق السريعة

قبل البدء، تأكد من:
- [ ] XAMPP مثبت
- [ ] Node.js مثبت
- [ ] MySQL يعمل في XAMPP
- [ ] قاعدة البيانات `blocksystem` موجودة
- [ ] ملف `.env` موجود ومُعد بشكل صحيح
- [ ] تم تنفيذ `npm install`
- [ ] تم تنفيذ `npm run db:push`
- [ ] تم تنفيذ `npx tsx server/seed.ts`
- [ ] التطبيق يعمل على `http://localhost:5000`

---

**🎉 مبروك! المشروع جاهز للاستخدام!**
